services:
  - type: web
    name: sg-buyback-monitor
    runtime: docker
    plan: free
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 3000
      - key: API_TOKEN
        value: sgbuyback2026
