services:
  - type: web
    name: sg-buyback-monitor
    runtime: docker
    plan: free
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 3000
      - key: CRON_SCHEDULE
        value: "0 */6 * * *"
